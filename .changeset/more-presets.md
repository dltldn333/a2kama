---
"a2kama": minor
"@a2kama/presets": minor
---

Add jelly, prism, aurora, bloom, ripple, heat haze, smoke and oil presets. Add `a2kama.setRecipe()` to swap an element's recipe at runtime, and make `getOptions()` reads return live uniform values.
