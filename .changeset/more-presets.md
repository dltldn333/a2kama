---
"a2kama": minor
"@a2kama/presets": minor
---

Add jelly, prism, aurora, bloom, holographic, ripple, heat haze, smoke, glitch and oil presets. Add `a2kama.setRecipe()` to swap an element's recipe at runtime, and make `getOptions()` reads return live uniform values.
